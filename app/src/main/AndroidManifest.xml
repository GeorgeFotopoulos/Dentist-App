<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.efarmoghgiaodontiatrous">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DentistViewProfileActivity"></activity>
        <activity android:name=".DentistMenuPresenter" />
        <activity android:name=".DentistMenuActivity" />
        <activity android:name=".DentistAdapter" />
        <activity android:name=".view.Client.SearchDentist.DentistSearchActivity" />
        <activity android:name=".view.Client.RequestAppointment.RequestAppointmentActivity" />
        <activity android:name=".view.Client.GuestMenu.GuestMenuActivity" />
        <activity android:name=".ui.login.DentistLoginActivity" />
        <activity android:name=".DentistSignupActivity" />
        <activity android:name=".view.HomePage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>